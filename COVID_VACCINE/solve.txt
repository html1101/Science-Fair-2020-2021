best_found = "abcbdhfhfhhs"
best_loss = loss(best_found)
this_found = best_found;
do
{   changed = false;
    for( int idx=0; idx<4000; idx++ )
    {
        for(int val=0; val<4; val++)
        {   this_found[idx] = val;
            this_loss = loss(this_found);
            if (this_loss < best_loss)
            {   printf("Best solution so far : %s, %f", this_found, this_loss);
                best_loss = this_loss;
                best_found = this_found;
                changed = true;
            }
        }
        this_loss = best_found;
    }
}
while(changed)